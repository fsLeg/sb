# Maintainer: Vladislav 'fsLeg' Borisov <fsleg at t-rg dot ws>

PRGNAM="dart-sass"
VERSION="1.105.0"
EMBEDDED_PROTOCOL_VERSION="3.3.0"
BUILD="${BUILD:-1}"
DESCRIPTION="Dart implementation of Sass"
HOMEPAGE="https://sass-lang.com/dart-sass"
DOWNLOAD="https://github.com/sass/dart-sass/archive/${VERSION}/${PRGNAM}-${VERSION}.tar.gz \
          https://github.com/sass/sass/archive/embedded-protocol-${EMBEDDED_PROTOCOL_VERSION}/sass-embedded-protocol-${EMBEDDED_PROTOCOL_VERSION}.tar.gz"
SHA256SUM="cc526648540511381187af2e71d7c831e7ad2ba27d8fb0d0d882e78053a4df79
           17ea26c8ae3bb03a7dc72f841d7d832b64410230483cdde8807ab4b7f9204ce8"
BUILD_REQUIRES=(buf dart-sdk)
SKIP_STRIP="1"

unpack () {
  $UNTAR $CWD/$PRGNAM-$VERSION.tar.gz
  $UNTAR $CWD/sass-embedded-protocol-$EMBEDDED_PROTOCOL_VERSION.tar.gz -C $PRGNAM-$VERSION
  mkdir -p $PRGNAM-$VERSION/build
  mv $PRGNAM-$VERSION/sass-embedded-protocol-$EMBEDDED_PROTOCOL_VERSION $PRGNAM-$VERSION/build/language
}

build () {
  dart --disable-analytics
  dart pub get
  UPDATE_SASS_PROTOCOL=false \
    dart run grinder protobuf
  dart compile exe \
       -Dversion=$VERSION \
       -Ddart-version=$(dart --version | cut -d' ' -f4) \
       -Dprotocol-version=$EMBEDDED_PROTOCOL_VERSION \
       -Dcompiler-version=$VERSION \
       -o sass \
       bin/sass.dart

  install -Dm755 -t $PKG/$_PREFIX/bin sass
  install -Dm644 -t $PKG/$_PREFIX/doc/$PRGNAM-$VERSION \
    README.md AUTHORS CHANGELOG.md CONTRIBUTING.md LICENSE
}

